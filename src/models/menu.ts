export interface MenuItem {
    id: string;
    label: string;
    icon: React.ReactNode;
    link: string;
}
export interface MenuAdminItem {
    key: string;
    label: string;
    icon: React.ReactNode;
    path: string;
}

